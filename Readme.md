## Node
Esse projeto usa o [Node](https://nodejs.org) versão 18.16.0.

## Para Gerar
npm init -y (NODE)

## Install Dependencies
npm install express (Express)
npm install mongoose (Mongoose) MongoDB
npm install mongoose (Mongoose) MongoDB
npm install nodemon --save-dev (restart with nodemon)